server:
  port: 8850
spring:
  application:
    name: dingtalk-service
  zipkin:
    base-url: http://106.12.184.126:8881
  datasource:
    druid:
      url: jdbc:mysql://127.0.0.1:3306/dingtalk?useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      username: root
      password: Wang#1990
      driver-class-name: com.mysql.cj.jdbc.Driver
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
mybatis:
  type-aliases-package: com.lanson.servicedingtalk.domain
  mapper-locations: classpath:mapper/*.xml


eureka:
  instance:
    hostname: 49.234.225.92
    prefer-ip-address: false
    instance-id: ${eureka.instance.hostname}:${server.port}
  client:
    service-url:
      defaultZone: http://106.12.184.126:8060/eureka/
